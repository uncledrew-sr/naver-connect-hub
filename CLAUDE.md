# Claude Code 안내

이 저장소의 프로젝트 규칙은 [AGENTS.md](./AGENTS.md)를 단일 기준으로 사용한다.
Claude Code도 작업을 시작하기 전에 `AGENTS.md`를 읽고 따른다.

Claude Code 전용 에이전트·명령은 `.claude/`에 있고, 공통 스킬은 `.agents/skills/`에
있다. 이 파일에는 공통 규칙을 복사하지 않는다. 공통 규칙을 변경할 때는
`AGENTS.md`만 수정한다.
